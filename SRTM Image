var image = ee.Image("CGIAR/SRTM90_V4");

//Map.centerObject(image, 9); //zoom 9

Map.setCenter(92.2,22.633,9); //latitude, longitude, zoom level

//Map.addLayer(image, {min: 0, max: 3000}, "custom visualization"); //display

Map.addLayer(image, {min: 0, max: 3000, palette: ['blue', 'green', 'red']}, "custome palette"); //display